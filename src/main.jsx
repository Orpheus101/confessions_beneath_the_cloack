import React from 'react';
import ReactDOM from 'react-dom/client';
import ConfessionGame from './confession_game';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ConfessionGame />
  </React.StrictMode>,
);
